//when the joystick is moving, grab ultrasonic (us) counts of each claw and apply that to a proportion

//this is all contingent on both claws starting at the exact same starting place

/*
	float leftClawSpeed = joystick axis value
	float rightClawSpeed = joystick axis value
	if (leftUS > rightUS)
	{
		leftClawSpeed = leftClawSpeed * (rightUS/leftUS);
	}
	else if (rightUS > leftUS)
	{
		rightClawSpeed = rightClawSpeed * (leftUS/rightUS);
	}
	//we apply that prportion to cut the speed and adjust them until they are almost the same
		//this allows the change of speeds to be small when they are only slightly off and large when they are way off
	leftClaw = clawSpeed;
	rightClaw = clawSpeed;
*/
